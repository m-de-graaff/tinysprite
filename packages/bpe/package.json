{
  "name": "@tinysprites/bpe",
  "version": "0.1.0",
  "description": "Byte-pair encoding compression for tinysprites (optional)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest",
    "clean": "rm -rf dist"
  },
  "sideEffects": false,
  "peerDependencies": {
    "@tinysprites/core": "workspace:*"
  },
  "keywords": ["sprites", "compression", "bpe", "js13k", "optional"],
  "license": "MIT"
}
