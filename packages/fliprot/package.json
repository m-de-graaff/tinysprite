{
  "name": "@tinysprites/fliprot",
  "version": "0.1.0",
  "description": "Flip and rotation transforms for tinysprites (optional)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest",
    "clean": "rm -rf dist"
  },
  "sideEffects": false,
  "peerDependencies": {
    "@tinysprites/core": "workspace:*"
  },
  "keywords": ["sprites", "transform", "flip", "rotate", "js13k", "optional"],
  "license": "MIT"
}
